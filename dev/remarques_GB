Sur la description (readme.md), spécifier qu'il s'agit d'une version de CropWat codée sous R, pour éviter la confusion avec la 
version standard de Cropwat (telle que celle mise à disposition par la FAO). Toutes les options de Cropwat ne sont pas disponibles.