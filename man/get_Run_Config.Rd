% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{get_Run_Config}
\alias{get_Run_Config}
\alias{get_Path_Run}
\title{Get or create a CAWET run configuration}
\usage{
get_Run_Config(
  path = NULL,
  Working_path = dirname(dirname(path)),
  Scenario = NULL
)

get_Path_Run(path = choose.dir(), Working_path, new = FALSE)
}
\arguments{
\item{path}{Character string specifying the path to an existing run directory.
If NA the function retrieves the most recent run directory.
By default, a folder selection dialog will be opened.}

\item{Working_path}{Character string specifying the working directory.}

\item{Scenario}{Optional data frame containing scenario data.
If provided, it will be saved to the run directory. If NULL, the function
will attempt to read the scenario data from the run directory.}

\item{new}{Logical indicating whether to create a new run directory.
If TRUE, a new run directory will be created. If FALSE, the most recent
existing run directory will be retrieved.}
}
\value{
\link{get_Run_Config} returns a \link{list} with the following elements:
\itemize{
\item \code{Working_path}: The working directory used for the run.
\item \code{Path_Run}: The path to the created or selected run directory.
\item \code{Scenario}: A data frame containing the scenario information used for the run.
\item \code{Path_Run_Chargementdata}: The path to the \code{Charged_inputs} directory inside the run.
}

\link{get_Path_Run} returns a character string with the path to the run directory.
}
\description{
Get or create a CAWET run configuration
}
\details{
Behavior depending on the value of \code{Scenario}:
\itemize{
\item If \code{Scenario} is provided (non-NULL):
\itemize{
\item \code{get_Run_Config} calls \code{get_Path_Run(..., new = TRUE)} which creates a new
run directory at \verb{file.path(Working_path, "Runs", paste0("Run_", <timestamp>))}.
\item The provided \code{Scenario} data frame is written into \code{Scenarii.csv} inside the
new run directory.
\item A \code{Charged_inputs} directory is created inside the run directory and its path is
returned as \code{Path_Run_Chargementdata}.
}
\item If \code{Scenario} is NULL:
\itemize{
\item \code{get_Run_Config} calls \code{get_Path_Run(..., new = FALSE)} which:
\itemize{
\item If \code{path} is NA: finds the most recent existing run directory under
\code{file.path(Working_path, "Runs")} (the last \code{Run_} entry).
\item If \code{path} is provided: validates that the directory exists and returns it.
}
\item The function attempts to read \code{Scenarii.csv} from the chosen run directory using
\code{read_ordonnanceur()} to populate \code{Scenario}.
\item The \code{Charged_inputs} directory is created if missing and its path returned.
}
}

Note: the default \code{path} uses \code{choose.dir()}; when creating a new run the run
timestamp ensures unique run folder names.
}
\section{Functions}{
\itemize{
\item \code{get_Run_Config()}: This function retrieves or creates a run configuration for CAWET,
including the run path, scenario data, and path to charged input data.

\item \code{get_Path_Run()}: Get or create a run path

}}
